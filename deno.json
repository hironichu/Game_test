{
    "compilerOptions": {

    },
    "tasks": {
        "run": "deno run -A --unstable --import-map=./import_map.json app.ts",
        "init": "deno run -A --unstable --import-map=./import_map.json init.ts",
        "build": "deno compile -A --unstable --import-map=./import_map.json app.ts && mv app.exe dist",
        "clean": "rm -rf dist"
    }
}